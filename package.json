{
  "name": "meeting-record-monorepo",
  "private": true,
  "description": "System-audio and microphone capture with meeting detection — monorepo root",
  "license": "MIT",
  "repository": "https://github.com/hoangvvo/meeting-record",
  "workspaces": [
    "node",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace meeting-record",
    "test": "npm run build && npm run test --workspace meeting-record",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "node-addon-api": "^8.9.0",
    "node-gyp": "^13.0.1",
    "oxfmt": "^0.61.0",
    "oxlint": "^1.76.0",
    "prebuildify": "^6.0.1",
    "typescript": "^5.4.0"
  }
}
